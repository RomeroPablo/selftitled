{
  "name": "selftitled",
  "version": "1.0.0",
  "scripts": {
    "vercel-build": "curl https://sh.rustup.rs -sSf | sh -s -- -y && export PATH=\"$HOME/.cargo/bin:$PATH\" && npm install wasm-pack && npx wasm-pack build --target web --out-dir ./web/pkg"
  }
}
